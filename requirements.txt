# Minimum-version dependencies for the Andean Obsidian Geochemistry notebook.
# Deliberately UNPINNED so pip can resolve compatible wheels on whatever Python
# version/OS is actually available (local Miniconda, WSL, Binder, etc.) instead
# of failing when an old exact pin has no prebuilt wheel. See environment.yml
# for the conda equivalent.
numpy>=1.24
pandas>=2.0
plotly>=5.10
ipywidgets>=8.0
jupyterlab>=4.0
jupyterlab-widgets>=3.0
widgetsnbextension>=4.0
anywidget>=0.9
voila>=0.5
